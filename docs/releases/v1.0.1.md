## Woo's NWN Log Parser v1.0.1

A tool for analyzing Neverwinter Nights combat logs with DPS calculations and detailed combat statistics.

### Download

Download `WoosNwnParser.exe` below and run it.

**Windows Security Notice**: Windows may show a SmartScreen warning because this is a new application. Click "More info" → "Run anyway" to proceed. This is normal for new, unsigned applications.

### Security Scan

VirusTotal: [2/71 vendors flagged](https://www.virustotal.com/gui/file/95ac2eb5a80e29ca78eb2c091881b0586fbd65b05ba138d55f912be9c4509dce)

These are false positives common with unsigned Python executables. The detections are generic ML-based flags, not specific malware signatures. Source code is available for inspection.

### Features

- Real-time combat log parsing
- DPS calculations with time tracking modes
- Damage type breakdown
- Target filtering
- Hit rate statistics
- AB, AC, Saves per target
- Damage immunities per target

### System Requirements

- Windows 10/11
- No installation required - just run the exe

### Changelog

#### Added
- Comprehensive test suite
- `README.md` file with app screenshots

#### Fixed
- **Target Immunities** panel: Coupled `Max Damage` and `Absorbed` (record both from same action) for accurate `Immunity %` calculation
- **Damage Per Second** panel: Fixed treeview expand/collapse indicator not changing when parent is expanded

#### Changed
- Moved icon file to `assets/icons/` for better encapsulation
- Rebuilt app using `pyarmor` for code obfsucation and `pyinstaller` with a self-built bootloader to reduce AVs false positives
